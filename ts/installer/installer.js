


class Installer {

    run( scope ) {

        // prompt for install location

        // prompt for user to install as

        /** Django Application Install Instructions **/

        // const installer = new DjangoInstaller()

        // installer.run(scope)


        /** Configure HTTPS **/

        /** Configure Cloudflare */
    }



}


class DjangoInstaller {

    init() {

    }

    run( scope ) {

        // ask which database to use

            // show installed database servers (Postgres, MySQL, SQLite)

            // install the database server if needed

                // including any necessary python modules

            // install the database

        // copy application files

        // create a virtual environment

        // install necessary django files

        // install requirements.txt


        // apply migrations

        // create a system service using gunicorn

        // create a site configuration files for nginx

    }

}

class AngularInstaller {

    run( scope ) {

        // copy the files

        // update the site configuration for nginx

    }
}